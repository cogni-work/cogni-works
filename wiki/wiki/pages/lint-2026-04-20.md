---
id: lint-2026-04-20
title: Lint Report — 2026-04-20
type: note
tags: [lint, maintenance]
created: 2026-04-20
updated: 2026-04-20
---

# Lint Report — 2026-04-20

## Summary

- 🔴 Errors: 0
- 🟡 Warnings: 166 mechanical + 2 semantic
- 🔵 Info points: 5
- Pages audited: 211 (mechanical) + sampled set (semantic)
- Semantic pass: yes

## 🔴 Errors

None. No broken wikilinks, no missing frontmatter fields, no missing required fields.

## 🟡 Warnings

### Architectural orphan pattern (160 pages)

All 160 `skill-*` and `agent-*` component pages have no inbound wikilinks. Root cause: parent plugin pages describe their components as prose lists (e.g., `plugin-cogni-portfolio.md` writes `products, features, markets...` as plain text) instead of using `[[skill-cogni-portfolio-products]]` wikilinks. Only two component pages are non-orphaned (`[[skill-cogni-portfolio-propositions]]` and `[[skill-cogni-portfolio-trends-bridge]]`), which are explicitly linked from their plugin page.

This is one structural fix (update plugin pages to use wikilinks in their Skills/Agents sections), not 160 individual edits.

### Probable tag typos (6)

- `skills` (2x) likely typo of `skill` (90x)
- `agents` (4x) likely typo of `agent` (76x)
- `b2b` (4x) likely typo of `fab` (22x) — **not a typo**; `b2b` is the correct market segment tag
- `web` (1x) likely typo of `fab` (22x) — **not a typo**; `web` is a real content-type tag
- `abm` (1x) likely typo of `fab` (22x) — **not a typo**; `abm` = account-based marketing
- `web` (1x) likely typo of `b2b` (4x) — **not a typo**

Real typos to fix: `skills` → `skill`, `agents` → `agent`. The other four are false positives from the Levenshtein heuristic.

### Contradictions (semantic)

- **Plugin count (2 places)** — `[[ecosystem-overview]]` line 13 says "13-plugin monorepo" but `[[index]]` (lines 67, 203) and marketplace.json list 14 plugins. cogni-wiki (the 14th) was added after ecosystem-overview was written. Reconcile via `wiki-update` on `ecosystem-overview`.

- **Stale reference to cogni-docs** — Three concept pages reference a `cogni-docs` utility plugin that does not exist in the repo or marketplace:
  - `[[concept-four-layer-architecture]]` — "cogni-docs — utility, generates documentation by reading every plugin's structure"
  - `[[concept-plugin-maturity-model]]` (referenced)
  - `[[concept-readme-convention]]` (referenced)

  The actual `docs/` directory is generated by cogni-docs-style skills likely hosted inside another plugin (or was renamed). Reconcile via `wiki-update` by either (a) adding the real plugin name, or (b) removing the reference if the capability is distributed across plugins now.

## 🔵 Info

- Total pages: 211 (18 concept, 11 summary, 180 entity, 2 note incl. this report)
- By type: concept 20, summary 11, entity 180
- Average sources per page: 1.09 — on the low side; many entity pages cite only one README URL
- Top tags: skill (90), agent (76), sonnet (44), cogni-portfolio (43), portfolio (42), cogni-visual (29), visual (29), fab (22), cogni-trends (18), trends (17)
- Most-linked pages: `[[concept-agent-model-strategy]]` (79), `[[plugin-cogni-portfolio]]` (44), `[[plugin-cogni-visual]]` (33), `[[plugin-cogni-trends]]` (19), `[[plugin-cogni-marketing]]` (17), `[[plugin-cogni-research]]` (17), `[[concept-data-isolation]]` (12), `[[concept-quality-gates]]` (11), `[[plugin-cogni-website]]` (11), `[[plugin-cogni-consulting]]` (10)

## Next actions

1. **Fix the two semantic contradictions first** via `wiki-update` — they are active misinformation: update `ecosystem-overview` to 14 plugins; remove or correct the `cogni-docs` references in the three concept pages.
2. **Decide on the orphan pattern.** Either (a) treat plugin-page prose-lists as intentional and accept 160 orphan warnings as baseline noise, or (b) update the 14 plugin pages to wikilink their component pages in the Skills/Agents sections. Option (b) is one pass over 14 files and eliminates the bulk of the warning count permanently.
3. **Two real tag typos** (`skills` → `skill`, `agents` → `agent`) can be swept with `wiki-update` in a single pass.
4. No urgent action on average-sources (1.09) — below ideal but not broken; raise when adding new content.
