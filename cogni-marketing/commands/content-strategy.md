---
name: content-strategy
description: Build the content matrix (market × GTM path × content type) with format recommendations
argument-hint: "[market-slug]"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Agent
---

Build or update the content strategy for the marketing project. If a market slug is provided, focus on that market. Otherwise, build strategy for all configured markets.

Load and follow the `content-strategy` skill from `${CLAUDE_PLUGIN_ROOT}/skills/content-strategy/SKILL.md`.
